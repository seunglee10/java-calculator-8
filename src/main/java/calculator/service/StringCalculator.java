package calculator.service;

public class StringCalculator {

    // 문자열을 입력받아 숫자를 추출하고 합을 반환하는 메소드
    public int add(String text) {
        if (text == null || text.isEmpty()) {
            return 0;
        }

        // 빈 문자열이 아닌 경우를 처리하기 위해 임시로 0을 반환
        return 0;

    }
}
